apiVersion: v1
kind: Service
metadata:
  name: inventory
  labels:
    app: inventory
    role: downstream
    demo: blast-radius
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
  selector:
    app: inventory
